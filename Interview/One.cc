#include <iostream>
/*
在 2D 笛卡尔坐标空间中。给定一个轴对齐矩形(每条边都与某个坐标轴平行)的中心点
坐标 rectangleCenter、长度 length(边平行于x轴)和宽度 width(边平行于y轴)，始
定一个圆形的中心点坐标 circleCenter 和半径 radius，判断这两个图形是否存在面积
相交(结果不包括边缘相切)。
输入    [ 0.0, 0.0], 2.0, 2.0, [1.0, 1.0], 1.0
输出    true
*/

// int *pi = new int[10];
// int val;
// const int *pi = val;
// int val;
// const int *pi = new int(val);
// int *pi = new int(1024);
